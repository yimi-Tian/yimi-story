import { expect, test, vi } from "vitest";
import { fetchPublicationSnapshots, requestPublicationPreparation } from "./publication-repository";

test("publication Edge request只送action、draft與expected revision",async()=>{const invoke=vi.fn().mockResolvedValue({data:{preparation:{valid:true,errors:[],warnings:[],validation:{revision:2,mediaCount:0,coverReady:true,exporterDeterministic:true},checksum:"a".repeat(64)},snapshot:{source_revision:2,checksum_sha256:"a".repeat(64),status:"ready",created_at:"2026-08-28T00:00:00Z"}},error:null});const client={functions:{invoke}} as never;const result=await requestPublicationPreparation(client,"draft-safe",2,"create");expect(invoke).toHaveBeenCalledWith("prepare-publication-snapshot",{body:{action:"create",draftId:"draft-safe",expectedRevision:2}});expect(result.snapshot?.revision).toBe(2);});
test("history僅讀draft snapshots並限制筆數",async()=>{const limit=vi.fn().mockResolvedValue({data:[{source_revision:3,checksum_sha256:"b".repeat(64),status:"ready",created_at:"2026-08-28T00:00:00Z"}],error:null});const order=vi.fn(()=>({limit})),eq2=vi.fn(()=>({order})),eq1=vi.fn(()=>({eq:eq2})),select=vi.fn(()=>({eq:eq1})),client={from:vi.fn(()=>({select}))} as never;const rows=await fetchPublicationSnapshots(client,"content-safe");expect(eq2).toHaveBeenCalledWith("snapshot_source","draft");expect(limit).toHaveBeenCalledWith(10);expect(rows[0].revision).toBe(3);});
